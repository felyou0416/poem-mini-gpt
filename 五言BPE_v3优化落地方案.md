# 五言古诗小模型 · 专家级优化落地路线图 (v3)

> **核心指导思想**：从第一性原理出发，解决当前小模型“**参数头重脚轻（字典吃掉 67% 参数）**”与“**BPE 子词分词破坏音步五言严格格律**”两大底层矛盾。
> 结合清华“九歌”系统经验与现代小语言模型（SLM）架构原则，构建兼具**神经生成创造力**与**符号格律约束力**的古典写诗引擎。

---

## ✅ 执行状态（2026-09-14 更新：v3 全部完成）

| 阶段 | 状态 | 关键产出 |
|---|---|---|
| **P0 推理端 Best-of-N** | ✅ **已完成** | `rhyme_dict.py`（十四韵 7,185 字 + 评分器）、`poem.py` Best-of-N 重排、前端韵部徽章高亮；API 端到端验证通过 |
| **P1a 词表瘦身** | ✅ **已完成** | `build_bpe_v3.py`：严格五言清洗 92,063 首，词表 **4,006**（Top4000 字符 + 6 特殊 token），覆盖率 98.7% |
| **P1b 架构重构** | ✅ **已完成** | `train_v3.py`：6 层/192 维/6 头 + RoPE + RMSNorm，344 万参数 |
| **P1c 全量预训练** | ✅ **已完成** | 92,063 首 / 6,000 步 WSD / 断点续训守护；**final loss 3.91 / val 3.95**（v2 为 6.22） |
| **P2 名篇 SFT** | ✅ **已完成** | 3,125 首名家五言（作者配额均衡）→ LR 5e-5 / 600 步早停 / Dropout 0.15 → `wuyan_bpe_v3_sft_model.pt`，val 3.39 |
| **P3 特殊 token** | ✅ **并入 P1** | `<|bos|><|title|><|author|><|body|><|eos|>` + `<|unk|>` 兜底，生成遇 eos 自动停机 |

> **执行中的关键工程决策（与文档原稿的差异，以本表为准）**：
> 1. 韵部资料采用中华诗词学会《中华新韵》2005 十四韵（华韵网版，7,185 字），多音字按“(又X韵)”标注跨部归并；清洗了原网页“放过/发动”两处排版错字；
> 2. 词表 = **Top 4,000 高频古诗字符（含《》、句读标点）+ 6 个特殊 token**（含 `<|unk|>` 兜底生僻字）= **4,006**。原稿 4,005 不含 unk；加 unk 使 9.2 万首语料**零丢失**全量编码（否则 44% 含生僻字的诗会被整首丢弃）；
> 3. 数据量 92,063 首（原稿 11.2 万）：严格五言过滤（每句恰 5 字）+ 剔除“诗题：”污染行 + 去重后，数据更纯净；每首保留原始句读（逗号/句号分布）；
> 4. P3 的 SwiGLU 暂缓（验收清单未要求），P1b 采用 GELU MLP 控制参数量与 CPU 训练时长；RoPE/RMSNorm/特殊token 停机已全部落地。

---

## 🗺️ 演进三部曲全景

```
【第一步：推理增强（神经-符号协同 · 零重训即刻见效）】
   现有 v2 模型 ──→ 平水韵/五言格律评分器 ──→ Best-of-N 候选重排 + 韵部标注 ──→ 及格率提升至 85%+
        │
【第二步：v3 主力模型架构重构（质变）】
   精选 4,000 活跃诗词汉字（告别BPE音步失真） ──→ 释放 90万参数 ──→ 升级 6层/192维 RoPE 现代架构 ──→ 11.2万首预训练
        │
【第三步：唐风美学对齐（SFT 防过拟合微调）】
   精选 3,000~5,000 首盛唐五言名家篇目 ──→ 极小学习率 (5e-5) 限制性微调 600 步 ──→ 注入纯正盛唐雅韵
```

---

## 📌 第一步 (P0)：推理端神经-符号协同（Best-of-N 韵律重排序）

### 1. 核心目标与技术背景
纯因果自回归模型是局部由前向后的概率采样，对跨句押韵（第2句与第4句尾字同一韵部）存在天然的暴露偏差（Exposure Bias）。
借鉴清华“九歌”系统的判别式后处理，构建**符号评分器（Symbolic Evaluator）**，在推理端让模型批量发散候选，通过严密规则筛选出格律最佳的诗篇。

### 2. 技术设计与打分系统
- **平水韵字典库 (`scripts/rhyme_dict.py`)**：
  - 覆盖常用古典诗词 2,500 个高频韵字；
  - 映射到平水韵核心韵部（如：上平一东、二冬、七阳、八庚、十一真、下平一先、七尤等）；
  - 附带平水韵平仄属性（平声 / 仄声）。
- **复合评分算法 (`score_poem`)**：
  1. **字数与格式分 (满分 30 分)**：
     - 正文每小句严格 5 个字（每多/少一字扣 15 分）；
     - 格式需具有完整的《标题》与作者。
  2. **押韵契合度 (满分 40 分)**：
     - 五言绝句提取第 2 句尾字与第 4 句尾字；
     - 同属一个平水韵部：满分 40 分，并在诗歌元数据中高亮该韵部（如 `[下平一先韵]`）；
     - 若首句尾字亦入韵：额外加 10 分。
  3. **去重与防呆惩罚 (最高 -40 分)**：
     - 句内重字惩罚：单句内出现非叠词的重复汉字（如“月夜多见月”），每个重复扣 20 分；
     - 意象滥用惩罚：整首诗超过 2 次出现“月”、“春”、“人”等泛滥高频字，每个扣 10 分。
- **采样流水线**：
  - 当用户请求 $N$ 首诗时，底层自回归采样 $K = \max(N \times 4, 8)$ 首；
  - 逐一评分，返回 Top-$N$ 高分作品。

---

## 📌 第二步 (P1 + P3)：v3 现代架构重构（纯字符表 + RoPE + 深度扩容）

### 1. 关键决策：放弃 BPE，回归“精炼纯字表”
- **BPE 的致命伤**：BPE 将“春风”、“孤舟”合为一个 token，导致模型无法显式数数，每句字数飘忽不定。
- **字符表的绝对优势**：中国古典诗歌“一字一音一拍”，五言诗严格等于 5 个 token。
- **词表瘦身与参数测算**：
  - 统计全量诗词，提取 Top 4,000 个高频古诗汉字（覆盖率高达 $99.85\%$）；
  - 加上 5 个特殊标记：`<|bos|>`, `<|title|>`, `<|author|>`, `<|body|>`, `<|eos|>`；
  - 词表大小锁定为 **4,006**（4,000 高频字符 + 6 特殊 token，含 `<|unk|>` 兜底）。

### 2. 模型骨架扩容对比

| 参数维度 | 现有 v2 模型 (BPE) | 新一代 v3 模型 (现代架构) | 提升与优势 |
|---|---|---|---|
| **分词方案** | BPE (12,816 词表) | **精炼古诗字表 (4,006 词)** | **彻底消除字数失真，每句严格 5 字** |
| **嵌入层参数** | 164 万 (占 67%) | **77 万 (仅占 22%)** | **省出近 90 万参数用于深度特征学习** |
| **层数 ($n\_layer$)** | 4 层 | **6 层** | 网络深度加深 50%，长程意象连贯性质变 |
| **隐藏维度 ($n\_embd$)** | 128 | **192** | 特征宽度扩充 50% |
| **注意力头数 ($n\_head$)** | 4 个头 | **6 个头 (每头 32 维)** | 多视角捕获对仗与上下文关联 |
| **位置编码** | 绝对位置 Embedding | **RoPE (旋转位置编码)** | 天然捕捉五言诗的“相对韵律周期 (2-3 停顿)” |
| **归一化层** | LayerNorm | **RMSNorm** | 移除均值中心化计算，训练提速 15% |
| **起止控制** | 靠换行硬切 | **`<|bos|>` / `<|eos|>`** | 生成遇 `<|eos|>` 自动停机，彻底告别残句 |
| **Transformer 骨架参数** | 80 万 | **约 245 万** | **算力与特征表达能力暴增 3.1 倍！** |
| **总参数量** | 245 万 | **约 322 万** | CPU 友好，单步约 1.3 秒，总耗时约 1.5 小时 |

---

## 📌 第三步 (P2)：唐风美学对齐（SFT 防过拟合微调）

### 1. 痛点分析
全量 11 万首诗歌中夹杂大量宋明代粗糙应酬诗、口水诗，导致预训练模型容易学到平庸的句式套路。

### 2. 精品语料库与防过拟合微调机制
- **数据源**：从全唐诗中精选李白、杜甫、王维、孟浩然、韦应物、刘长卿、李商隐等名家传世名篇，以及《唐诗三百首》五言篇目，构建包含约 3,000 首盛唐/中唐巅峰之作的 `sft_tang_corpus.txt`。
- **训练超参数调优（防死记硬背）**：
  - 学习率降至 $5\times 10^{-5}$（Base 模型的 1/10）；
  - 步数严格限制在 500 ~ 800 步（约 15 分钟）；
  - 引入 Dropout=0.15；
  - 严密监控验证集 Loss，一旦 Loss 不再下降立即触发早停（Early Stopping），确保模型学到的是“盛唐诗意与遣词偏好”，而非“机械背诵原诗”。

---

## ⏱️ 落地执行规划

1. **第一阶段 (P0)**：编写 `scripts/rhyme_dict.py`（十四韵 7,185 字 + 评分器），Best-of-N 机制接入 `poem.py` 与 `api_server.py`，前端韵部徽章高亮 —— ✅ 已完成并端到端验收；
2. **第二阶段 (P1+P3)**：`build_bpe_v3.py` 构建 4,006 字表 + 特殊 token 数据（9.2 万首严格五言），`train_v3.py` 实现 6 层/192 维 RoPE + RMSNorm 的 `MiniGPT_v3`，6,000 步全量预训练（约 2.6 小时，守护续训）—— 🔄 训练中；
3. **第三阶段 (P2)**：基于 v3 检查点执行 15 分钟唐诗 SFT 微调（全唐诗+唐诗三百首五言名篇 3,000+ 首，LR 5e-5，早停），并在前端提供 v3 体验选项 —— ⏳ 待 P1 完成。
